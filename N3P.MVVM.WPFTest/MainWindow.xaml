<Window x:Class="N3P.MVVM.WPFTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding SubModel.Value, Mode=TwoWay}" />
        <Button Grid.Row="1" Grid.RowSpan="2"  Command="{Binding ChangeValueCommand}">Change Value</Button>
        <Button Grid.Row="1" Grid.Column="1" Command="{Binding UndoCommand}">Undo</Button>
        <Button Grid.Row="2" Grid.Column="1" Command="{Binding RedoCommand}">Redo</Button>
    </Grid>
</Window>
